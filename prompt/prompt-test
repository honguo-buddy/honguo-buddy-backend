Role: Senior Test Architect & CI/CD Coverage Engineer
Task: 为“红果校园帮帮”项目构建大厂级分布式测试高压防御网，终极目标是实现近 100% 的业务分支覆盖率。

【⚠️ 核心红线：严密死锁项目统一响应与错误码规范】
整个项目的接口均被全局异常处理器接管，所有响应结构体必须严格对齐以下契约。你在编写测试断言（Assert）时，严禁去盲目校验 HTTP 状态码（如 401, 403, 422），必须全面改为对内联业务 `code` 和 `message` 的深度解构校验：

1. 成功响应断言标准：
   - `assert body["code"] == 0`
   - `assert isinstance(body["message"], (dict, list))` (返回具体的业务载荷实体)

2. 异常响应断言标准：
   - 权限不足/越权企图：断言 `body["code"] == 102` (INSUFFICIENT_AUTHORITY)
   - 认证失效/未登录：断言 `body["code"] == 105` (TOKEN_INVALID)
   - 参数错误/业务违规：断言 `body["code"] == 99` (REQ_ERROR)
   - 异常结构体对账：必须严格断言 `body["message"]` 包含 `"error"` 和 `"msg"` 两个二级字段，例如：
     `assert "error" in body["message"]`
     `assert "msg" in body["message"]`

=====================================================================
一、 大厂级“四维象限”测试法（用例合规要求）
=====================================================================
你编写的每一个路由集成测试文件，必须强制包含以下四个维度的用例，严禁只测快乐路径：
1. 快乐路径契约：验证 `code=0`，且返回字段 100% 对齐 Pydantic Schema 的富返回。
2. 权限阻断边界：模拟无 Token 访问（期望 `code=105`），以及携带普通用户 Token 企图篡改/删除他人私有资源（期望 `code=102`）。
3. 参数防腐拦截：输入负数价格、超长文本、不存在的外键 ID，触发 BusinessHTTPException，断言拦截响应（期望 `code=99` 且结构体完整）。
4. 状态机副作用审计：验证撤回消息后，消息实体 `is_recalled` 是否翻转，且会话表的 `last_message_content` 联动被清洗。

=====================================================================
二、 100% 覆盖率工程硬性指标
=====================================================================
1. 分支覆盖 (Branch Coverage)：每一个 `if/elif/else` 分支、每一个 `try/except` 捕获块、每一条 Pydantic 的可选校验路径，都必须有对应的用例。
2. 自动化门禁支持：必须支持通过 pytest 命令行追加 `--cov=app --cov-branch --cov-report=xml:test_reports/coverage.xml` 指令，供 Jenkins 精准抓取分支覆盖率。
3. 质量告警清零：所有 `DeprecationWarning`、`ResourceWarning`、`SAWarning` 等运行/测试告警都视为质量缺陷，必须在测试代码或实现代码中消除，禁止仅靠忽略或屏蔽处理。

=====================================================================
三、 目录结构与物理隔离规范
=====================================================================
必须在根目录下创建 `tests/` 目录，执行严格的物理隔离：
tests/
├── conftest.py              # 全局共享独立基础 Fixtures (app 实例, fake_redis 等)
├── unit/                    # 【单元测试层】执行毫秒级纯内存断言（严禁连接数据库或拉起路由）
│   ├── core/                # 测试加解密、JWT、时间工具
│   └── schemas/             # 测试 Pydantic 模型模型内部的纯正则校验
└── integration/             # 【集成测试层】端到端链路协同测试
    ├── api/                 # 接口全协同测试 (Route -> Service -> DB -> Response)
    └── conftest.py          # 强控会话级临时动态随机建库与函数级 Session 回滚（🚨 严禁使用 Testcontainers 或硬编码固定库名）

=====================================================================
四、 具体交付用例示范
=====================================================================
- `tests/integration/api/test_auth_api.py`: 
  - 用例 1：使用 `swagger-login` 传入正确账密，验证 `body["code"] == 0` 并成功释放 Token。
  - 用例 2：传入非法密码，验证 `body["code"] == 101` (AUTH_FAILED)。
- `tests/integration/api/test_post_api.py`:
  - 用例 1：发帖成功，验证全量富返回。
  - 用例 2：A 用户企图去修改 B 用户的帖子，强行断言 `body["code"] == 102`，且 `body["message"]["error"]` 包含提示文案。
  - 用例 3：输入负数价格，断言拦截并验证 `body["code"] == 99`。

请立即开始，扮演最严格的黑客与QA，按照此最新修正的 API 规范编写高压防御测试代码！