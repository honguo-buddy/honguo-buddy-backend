🤖 重构版：红果帮帮全量测试体系构建指令 (V3.0)
1. 核心定义与准则 (Crucial)
你现在的身份是 资深测试架构师。你必须为“红果校园帮帮”项目构建两层防御体系，并严禁混淆：

单元测试 (Unit Tests)：

目标：测试孤立的函数或类逻辑（如：加密算法、信用分加权、数据校验）。

禁令：严禁触发 FastAPI 路由、严禁连接数据库、严禁触发中间件。

手段：大量使用 unittest.mock 模拟外部依赖。

集成测试 (Integration Tests)：

目标：测试模块间的协作（如：接口完整流转、数据库增删改查闭环）。

手段：使用 httpx.AsyncClient 请求接口，使用 dependency_overrides 替换测试数据库。

2. 目录结构规范
必须在根目录创建 tests/，并强制执行以下物理隔离：

Plaintext
tests/
├── conftest.py              # 全局共享 Fixtures (如 app 实例)
├── unit/                    # [单元测试层] 镜像映射 app/ 目录结构
│   ├── services/            # 测试纯业务逻辑函数
│   ├── core/                # 测试工具函数 (Security, Utils)
│   └── schemas/             # 测试 Pydantic 模型验证
└── integration/             # [集成测试层] 关注链路与协作
    ├── api/                 # 接口端到端测试 (从 Route 到 DB)
    ├── db/                  # 核心 CRUD 逻辑测试
    └── conftest.py          # 集成测试专属 Fixtures (如 Test DB Session)
3. 技术标准
异步框架：统一使用 pytest + pytest-asyncio。

Mock 策略：单元测试中，所有对数据库的调用必须被 Mock。

环境隔离：集成测试必须在 conftest.py 中覆盖数据库依赖，确保测试运行在真实数据库中（Testcontainers MySQL）中且测试后自动 Rollback。

4. 具体交付任务 (Deliverables)
A. 全局配置
tests/conftest.py: 定义基础 app 实例及不涉及数据库的全局 Fixtures。

B. 单元测试示例 (Unit)
tests/unit/core/test_security.py: 测试 create_access_token 函数（确保 JWT 载荷正确，不调接口）。

tests/unit/schemas/test_user_schema.py: 测试用户输入模型的正则验证逻辑。

C. 集成测试示例 (Integration)
tests/integration/api/test_auth_api.py: 模拟 swagger-login 完整请求。流程：Request -> Route -> Service -> DB -> Response。

tests/integration/api/test_user_info.py: 测试携带 Token 请求用户信息，需 Mock 掉微信环境但保留 Service 和 DB 逻辑。

D. 自动化集成 (CI/CD)
修改 Jenkinsfile：在 Build 阶段后新增 Test 阶段。

分层运行：要求先跑单元测试（快速失败），再跑集成测试。

报告生成：分别生成 unit_report.xml 和 integration_report.xml（JUnit 格式）并归档。

5. 初始突破口
由于目前登录依赖微信，请在 tests/integration/api/ 的测试中，利用项目预留的 swagger-login 调试接口绕过微信授权，生成测试用 Token，或在 dependency_overrides 中直接 Mock 掉权限验证依赖。