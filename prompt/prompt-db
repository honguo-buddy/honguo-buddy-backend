# Role: 高级 Python 后端工程师 (FastAPI + SQLAlchemy 2.0 专家)

# Context
我们正在开发一个名为“红果校园帮帮 (honguo-buddy)”的校园互助与二手交易综合平台。前端使用微信小程序，后端使用 FastAPI + SQLAlchemy 2.0 + MySQL。

# Objective
请在后端的 `/app/models` 目录下，根据以下业务需求和数据库规范，编写完整的 SQLAlchemy 数据库模型代码。请将不同业务域的模型分文件编写（例如 user.py, post.py, order.py 等），并在 `__init__.py` 中暴露它们以便 Alembic 识别。

# 🛠️ 核心开发规范 (严格执行)
1. 命名规范：
   - 表名 (`__tablename__`) 必须是**小写、单数**（如 `user`, `post`, `order`）。
   - 主键列命名必须是 `表名_id`（如 `user_id`），类型统一使用 `BigInteger, primary_key=True, autoincrement=True`。
   - 外键列命名必须是 `关联表名单数_id`（如 `publisher_id`, `buyer_id`）。
2. 审计与软删除机制：
   - 每张业务表必须包含：`create_time` (DateTime, default=func.now()), `update_time` (DateTime, default=func.now(), onupdate=func.now())。
   - 重要表必须包含 `is_deleted` (Boolean, default=False) 实现软删除。
3. 枚举类型规范：
   - 所有状态、类型字段必须在模型同文件内使用 Python 的 `enum.Enum` 定义。
   - SQLAlchemy 模型中使用 `Enum(MyEnumClass, native_enum=False)`，在 MySQL 中存为 VARCHAR，提高扩展性。
4. ORM 关系映射：
   - 必须使用 `relationship()` 建立表间关系，并严格配置 `back_populates`。
   - 针对高频关联查询，合理设置 `lazy="selectin"`。

# 📊 数据库表设计大全与业务逻辑

请生成以下 9 张表及其完整关系(已有的无需生成)：

1. `user` (用户表)
   - 字段：`user_id`, `user_uuid` (BINARY(16), 唯一), `user_name`, `avatar`, `sex` (枚举), `email`, `phonenumber`, `user_type` (枚举), `credit_score` (Integer, 默认100), `is_verified` (Boolean), `wechat_openid` (唯一), `wechat_session_key`, `is_deleted`, 审计时间。
   - 关系：`posts`, `goods`, `orders_as_buyer`, `orders_as_seller`, `comments`。

2. `category` (分类表 - 用于 JSON 模板驱动)
   - 字段：`category_id`, `name`, `icon`, `config_json` (JSON 类型，存储 UI 渲染和字段校验模板), `is_deleted`, 审计时间。

3. `post` (委托/任务表 - 包含代跑腿、技能等)
   - 字段：`post_id`, `publisher_id` (FK user), `category_id` (FK category), `title`, `price` (Decimal), `template_data` (JSON 类型，用于存储扩展的模板表单数据), `status` (枚举: OPEN, IN_PROGRESS, CLOSED, CANCELLED), `direction` (枚举: SELL, BUY), `expire_time` (DateTime), `is_deleted`, 审计时间。
   - 关系：`user`, `category`, `orders` (一对多或一对一), `comments`。

4. `goods` (二手商品表 - 实体物品)
   - 字段：`goods_id`, `publisher_id` (FK user), `category_id` (FK category), `name`, `description`, `price` (Decimal), `condition` (枚举: NEW, EXCELLENT, GOOD, FAIR), `is_sold` (Boolean), `is_deleted`, 审计时间。
   - 关系：同 post。

5. `order` (交易订单表 - 核心枢纽)
   - 字段：`order_id`, `buyer_id` (FK user), `seller_id` (FK user), `item_type` (枚举: POST, GOODS - 实现多态), `item_id` (BigInteger, 对应 post_id 或 goods_id), `amount` (Decimal), `status` (枚举: PENDING, ONGOING, CONFIRMED, COMPLETED, DISPUTED), `is_deleted`, 审计时间。
   - 关系：建立与 `buyer`, `seller` 的双向关系。

6. `comment` (评论表 - 多态与嵌套)
   - 字段：`comment_id`, `user_id` (FK user), `target_type` (枚举: POST, GOODS, ORDER), `target_id` (BigInteger), `parent_id` (FK comment.comment_id, nullable, 用于嵌套回复), `content` (Text), `is_deleted`, 审计时间。
   - 关系：`replies` (自关联)。

7. `credit_log` (信用流水表)
   - 字段：`log_id`, `user_id` (FK user), `change_amount` (Integer), `reason` (String), `create_time`。

8. `attachment` (多媒体附件表)
   - 字段：`attachment_id`, `target_type` (枚举: POST, GOODS), `target_id` (BigInteger), `url` (String), `create_time`。

9. `user_access_log` (用户访问日志表)
   - 字段：`log_id`, `user_id` (FK user), `action` (String), `ip_address` (String), `create_time`。

# 🚀 业务估量与索引设计要求 (Index Strategy)
请在相应的模型中使用 `index=True` 或 `Index()` 显式创建以下索引以应对高并发：
1. `user` 表：对 `user_uuid`, `wechat_openid` (高频登录), `email` 建立唯一索引。
2. `post` & `goods` 表：
   - 针对首页信息流，建立复合索引：`(status, is_deleted, create_time DESC)`。
   - 建立单列过滤索引：`category_id`, `publisher_id`。
3. `order` 表：建立 `buyer_id`, `seller_id` 索引，以及多态查询的复合索引 `(item_type, item_id)`。
4. `comment` 表：建立多态目标查询索引 `(target_type, target_id)`，和嵌套查询索引 `parent_id`。

请直接输出包含完整 imports、模型类和枚举定义的高质量 Python 代码。