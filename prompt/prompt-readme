📝 honguo-buddy-backend README 编写规范 Prompt
1. 任务背景
你现在是 honguo-buddy-backend（红果校园帮帮） 项目的文档工程师。你需要基于我提供的项目架构图 和既有的 API 规范，完全按照以下定义的四个核心模块结构来编写或续写项目的 README.md 文档。

2. 文档结构要求 (必须严格遵守顺序与标题级别)
一、 开发环境与部署 (Environment & Running)
环境依赖：明确 Python 3.11+、FastAPI、SQLAlchemy (Async)、Redis。

包管理工具：推荐使用 uv。

本地启动：提供 uv run uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload 命令。

配置说明：列出 .env 必要项（DATABASE_URL, REDIS_URL, WX_APP_ID, WX_APP_SECRET, SECRET_KEY 等）。

二、 项目架构 (Project Structure)
请基于以下目录结构进行描述，体现“职责分离”原则：

app/api/：路由定义（严禁定义 BaseModel）。

app/schemas/：Pydantic 请求/响应模型。

app/services/：核心业务逻辑。

app/models/：SQLAlchemy 数据库实体。

app/core/：全局配置、安全校验、异常处理器。

三、 接口统一规范 (API Standards)
必须保留并准确引用以下格式：

响应 JSON 结构：{"code": int, "message": object | list | str}。

核心错误码：SUCCESS: 0, UNKNOWN: 97, REQ_ERROR: 99, AUTH_FAILED: 101, TOKEN_INVALID: 105 等。

异常处理：明确 AuthHTTPException, BusinessHTTPException, ResourceHTTPException 的使用场景。

四、 接口列表 (API List)
此部分必须按路由模块分级。每一项必须包含：用途描述、请求方式/路径、请求示例(JSON)、成功响应示例、常见错误说明。

- 若接口需要权限控制，必须在用途或请求头段额外明确：例如“仅管理员可访问”、“仅帖子拥有者可操作”、“仅订单相关方可查看”等。
- 请求字段中若存在可选/非必填字段，必须在接口说明中明确标注 `可选`，并在示例中体现该字段为可选。
- 如果请求体字段均为可选，应说明“全部字段可选”或“请求体可以只包含部分字段”。
- 所有“请求示例”必须使用 JSON 格式编写，包含在 ```json ``` 代码块中；GET/查询参数接口请用 JSON 对象表示参数键值对。

4.1 AUTH 认证模块 (如：微信登录、Swagger 后门登录)

4.2 USER 用户模块 (如：获取/更新个人资料、校友身份认证)

4.3 POSTS 发布模块 (如：二手发布、跑腿委托、征集帖 1:N 逻辑)

4.4 CHAT 聊天模块 (如：私聊发起、临时群聊拉取)

...（以此类推）

3. API 描述模版 (参考范式)
当你在“接口列表”中新增接口时，请严格执行以下 Markdown 格式：

4.x.y 接口名称 (Method: /path)
用途: 简要说明。
请求头: Authorization: Bearer <token> (若需要)。
请求示例:

JSON
{ "field": "value" }
成功响应:

JSON
{ "code": 0, "message": { "detail": "操作成功" } }
常见错误:

code: 105 - Token 失效。

4. 当前执行任务
请针对 [此处填入你想要 AI 生成的具体模块，例如：4.3 POSTS 发布模块]，根据我们之前讨论的数据库设计（支持 1:N 征集和临时群聊逻辑），生成对应的 README 接口文档内容。