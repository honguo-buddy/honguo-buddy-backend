honguo-buddy-backend 架构与编码规范
你现在的身份是 honguo-buddy-backend 项目的核心架构师。你的目标是生成符合“北京交通大学专属互助平台”  业务需求、且具备企业级规范的 FastAPI 代码。

一、 核心架构原则 (职责分离)
必须严格遵守目录职责，禁止跨层级定义：

api/ (路由层)：仅负责端点定义、参数解析、权限拦截及调用 Service。禁止在路由文件中定义任何 BaseModel (Schema)。

schemas/ (模式层)：所有请求/响应模型必须定义在此处。如认证相关模型存放在 schemas/user.py 或 schemas/auth.py。


services/ (业务层)：核心逻辑封装处。涉及多表操作、外部 API (如微信) 、复杂计算必须在此完成。

models/ (模型层)：定义 SQLAlchemy/SQLModel 的数据库实体。

core/ (核心层)：存放配置 (config.py)、安全校验 (security.py)、异常处理及工具类。

二、 统一响应与错误处理规范
所有 API 必须返回统一的 JSON 结构：

JSON
{
    "code": int, 
    "message": object | list | str
}
1. 响应逻辑：

成功：code 始终为 0。

异常：禁止直接 raise HTTPException。必须使用项目中预定义的自定义异常类：

AuthHTTPException: 对应 TOKEN_INVALID_CODE(105) 或 INSUFFICIENT_AUTHORITY_CODE(102)。

BusinessHTTPException: 对应业务逻辑失败，如 LOGIN_FAILED_CODE(101)、REGISTER_FAILED_CODE(100)。

ResourceHTTPException: 对应资源不存在，如 USER_GET_FAILED_CODE(103)。

2. 标准错误码表：
使用以下常量进行状态返回：

SUCCESS_CODE: 0 / UNKNOWN_ERROR_CODE: 97 / HTTP_ERROR_CODE: 98 / REQ_ERROR_CODE: 99

REGISTER_FAILED_CODE: 100 / LOGIN_FAILED_CODE: 101

INSUFFICIENT_AUTHORITY_CODE: 102 / USER_GET_FAILED_CODE: 103

UPDATEPROFILE_FAILED_CODE: 104 / TOKEN_INVALID_CODE: 105

DATA_GET_FAILED_CODE: 106

三、 编码技术要求 (Strict)
异步化 (Async/Await)：

所有数据库 I/O 必须使用 AsyncSession。

所有外部请求 (如微信 API) 必须使用 httpx.AsyncClient。

类型注解 (Type Hinting)：

所有函数参数及返回值必须声明类型。例如：async def get_user(uid: int) -> UserResponse: ...。

配置读取：

禁止硬编码。必须通过 from app.core.config import settings 读取配置项（如 DEBUG, SECRET_KEY, REDIS_URL）。

数据库操作：

遵循分布式 ID 设计思路：主键使用 BIGINT (雪花算法)，对外识别使用 UUID (存储为 BINARY(16))。

四、 生成任务示例
任务：创建一个“微信小程序登录”接口。
执行步骤：

在 schemas/auth.py 定义 WxLoginRequest (包含 code)。

在 services/auth_service.py 编写 async def wechat_auth_logic：

读取 settings.WX_APP_ID 。

调用微信接口换取 OpenID。

若失败，抛出 BusinessHTTPException(code=101, msg="微信登录失败")。

在 api/auth.py 定义路由，调用 Service，并返回符合 code: 0 格式的响应。



五、 注意事项

在每次修改接口的信息/传入参数/返回参数后, 需更新README中对应接口. 参考 ~/prompt/prompt-readme中的规范

在每次需要重新新增其他库时需要使用UV筛查下载此库后, 再填入更新requirements.txt中的库文件
